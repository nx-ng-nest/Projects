import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'client-inventory',
  webDir: '../../dist/apps/client-inventory',
  bundledWebRuntime: false,
};

export default config;
